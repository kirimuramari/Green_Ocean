import { Ionicons } from '@expo/vector-icons';
import { View } from 'react-native';

export default function Test() {
  return (
    <View style={{ marginTop: 100 }}>
      <Ionicons name="home" size={100} color="red" />
    </View>
  );
}
